# =============================================================
# ScamShield — Production Requirements
# Python 3.11.x  |  Render Platform (Debian Linux)
# =============================================================
#
# RENDER SYSTEM PACKAGES REQUIRED (declare in render.yaml):
#   - poppler-utils   → required by pdf2image
#   - tesseract-ocr   → required by pytesseract
#
# All versions below are pinned to the exact versions that are
# verified working in the local .venv (Python 3.11.9).
# =============================================================

# ── Web Framework & WSGI Server ───────────────────────────────
flask==3.0.3
gunicorn==21.2.0
Werkzeug==3.0.0

# ── Database & Environment ────────────────────────────────────
supabase==2.31.0
python-dotenv==1.0.1

# ── Machine Learning & NLP ────────────────────────────────────
scikit-learn==1.4.2
numpy==1.26.4
scipy==1.14.1
joblib==1.4.2

# ── Data Processing ───────────────────────────────────────────
pandas==2.2.2

# ── PDF & Document Analysis ───────────────────────────────────
# Requires system: poppler-utils (pdf2image) + tesseract-ocr (pytesseract)
pdfplumber==0.10.3
PyMuPDF==1.23.8
pdf2image==1.16.3
Pillow==10.1.0
pytesseract==0.3.10
reportlab==4.2.5

# ── HTTP Clients & Web Scraping ───────────────────────────────
requests==2.31.0
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.2.2
tenacity==9.1.4

# ── Domain Intelligence ───────────────────────────────────────
python-whois==0.9.6

# ── Compatibility Pins ────────────────────────────────────────
# urllib3 1.x required: requests 2.31 + supabase auth chain is
# tested against 1.26.x; urllib3 2.x has breaking API changes.
urllib3>=1.26.20,<2.0